spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kemshop
    username: root
    password: 250104
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true